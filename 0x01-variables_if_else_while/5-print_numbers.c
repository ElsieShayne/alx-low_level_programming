#include <stdio.h>
/**
 * main - this program prints all single digit base 10 numbers.
 *
 * Return: Always 0 (success)
*/
int main(void)
{

	int y;

	while (y <= 9)
	{
		printf("%d", y);
			y++;
	}

	printf("\n");
	return (0);
}
