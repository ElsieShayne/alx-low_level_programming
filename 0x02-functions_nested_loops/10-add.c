#include "main.h"
/**
 * add - main entry point. Adds two integers.
 *
 * @x: first integer
 * @y: second integer
 * Return: the integer value of result.
*/
int add(int x, int y)
{
	return (x + y);
}
